# require 'rails_helper'
#
# RSpec.describe Branch, :type => :model do
#   it 'has a valid factory' do
#     expect(build(:branch)).to be_valid
#   end
#
#   let(:branch) { build(:branch) }
#
#   describe "Attributes" do
#     it { expect(branch).to respond_to(:first_name) }
#     it { expect(branch).to respond_to(:last_name) }
#     it { expect(branch).to respond_to(:created_at) }
#     it { expect(branch).to respond_to(:updated_at) }
#   end
